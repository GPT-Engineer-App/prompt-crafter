import { Box, Heading, Text } from "@chakra-ui/react";

const AgentPromptGenerator = () => {
  return (
    <Box p={4}>
      <Heading as="h1" mb={4}>Agent Prompt Generator</Heading>
      <Text fontSize="lg">Generate sophisticated prompts for agent-based frameworks.</Text>
    </Box>
  );
};

export default AgentPromptGenerator;