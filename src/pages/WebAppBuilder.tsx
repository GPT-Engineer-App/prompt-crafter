import { Box, Heading, Text } from "@chakra-ui/react";

const WebAppBuilder = () => {
  return (
    <Box p={4}>
      <Heading as="h1" mb={4}>Web App Builder</Heading>
      <Text fontSize="lg">Build full-fledged web applications using OpenAI Azure API.</Text>
    </Box>
  );
};

export default WebAppBuilder;